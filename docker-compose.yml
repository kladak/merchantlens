services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - MERCHANTLENS_ORDERS=2000
  web:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - api
